@{
    // Выбираем макет в зависимости от контекста запроса
    if (ViewData["IsAdminLayout"] as bool? == true)
    {
        Layout = "_AdminLayout";
    }
    else
    {
        Layout = "_Layout";
    }
} 