@{
    ViewData["Title"] = "Главная";
}

<div class="hero-section">
    <div class="container">
        <div class="row">
            <div class="col-md-8 mx-auto text-center">
                <h1 class="hero-title">Airlogistics</h1>
                <p class="hero-subtitle">Эффективное управление рейсами, самолетами и билетами</p>
            </div>
        </div>
        
        <div class="row">
            <div class="col-lg-10 mx-auto">
                <div class="search-card">
                    <form asp-controller="Home" asp-action="SearchFlights" method="get">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="destination">Пункт назначения</label>
                                    <input type="text" id="destination" name="destination" class="form-control" placeholder="Введите название города или аэропорта" required />
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="departureDate">Дата вылета</label>
                                    <input type="date" id="departureDate" name="departureDate" class="form-control" value="@DateTime.Today.ToString("yyyy-MM-dd")" />
                                </div>
                            </div>
                        </div>
                        <div class="row mt-3">
                            <div class="col-md-12 d-flex align-items-end">
                                <button type="submit" class="btn btn-primary w-100">
                                    <i class="fas fa-search"></i> Найти рейсы
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="container mt-5">
    <div class="row">
        <div class="col-md-4 mb-4">
            <div class="modern-card">
                <div class="card-body">
                    <h5 class="card-title">
                        <i class="fas fa-plane text-primary"></i> Самолеты
                    </h5>
                    <p class="card-text">Управление данными о самолетах, их характеристиках и вместимости.</p>
                    <div class="d-grid">
                        <a asp-controller="Aircraft" asp-action="Index" class="btn btn-primary">Перейти</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="modern-card">
                <div class="card-body">
                    <h5 class="card-title">
                        <i class="fas fa-route text-primary"></i> Рейсы
                    </h5>
                    <p class="card-text">Управление рейсами, расписанием и маршрутами полетов.</p>
                    <div class="d-grid">
                        <a asp-controller="Flight" asp-action="Index" class="btn btn-primary">Перейти</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="modern-card">
                <div class="card-body">
                    <h5 class="card-title">
                        <i class="fas fa-ticket-alt text-primary"></i> Билеты
                    </h5>
                    <p class="card-text">Продажа билетов и управление бронированиями мест на рейсы.</p>
                    <div class="d-grid">
                        <a asp-controller="Ticket" asp-action="Index" class="btn btn-primary">Перейти</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-md-6 mb-4">
            <div class="modern-card">
                <div class="card-header">
                    <h5><i class="fas fa-chart-line text-primary"></i> Отчеты</h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a asp-controller="Home" asp-action="TicketSalesReport" 
                           asp-route-startDate="@DateTime.Today.AddMonths(-1).ToString("yyyy-MM-dd")" 
                           asp-route-endDate="@DateTime.Today.ToString("yyyy-MM-dd")" 
                           class="btn btn-outline-primary">
                            <i class="fas fa-file-invoice-dollar"></i> Отчет о продаже билетов
                        </a>
                        <a asp-controller="Home" asp-action="FlightLoadReport" class="btn btn-outline-primary">
                            <i class="fas fa-percentage"></i> Отчет о загруженности рейсов
                        </a>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6 mb-4">
            <div class="modern-card">
                <div class="card-header">
                    <h5><i class="fas fa-plane-arrival text-primary"></i> Посадки</h5>
                </div>
                <div class="card-body">
                    <p>Управление данными о посадках рейсов в промежуточных и конечных пунктах.</p>
                    <div class="d-grid">
                        <a asp-controller="Landing" asp-action="Index" class="btn btn-primary">
                            <i class="fas fa-list"></i> Список посадок
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 